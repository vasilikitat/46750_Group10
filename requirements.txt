# Python dependencies for Assignment 1 (pip). Install with:  pip install -r requirements.txt
# A Gurobi licence is needed on top of the gurobipy package - see README.md.

gurobipy>=11.0          # optimisation solver interface
numpy>=1.26
pandas>=2.1
matplotlib>=3.8

# Optional: notebooks
jupyterlab>=4.2
ipykernel>=6.29
